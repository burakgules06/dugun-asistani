# ===================================================================
# UYGULAMA
# ===================================================================
spring.application.name=venueCrmAPP
server.port=8080

# ===================================================================
# VERITABANI
# ===================================================================
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/dugun}
spring.datasource.username=${DB_USER:dugun_app}
spring.datasource.password=${DB_PASSWORD:dev_sifre_123}

spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1

# ===================================================================
# JPA / HIBERNATE
# ===================================================================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.jpa.show-sql=false

# ===================================================================
# FLYWAY
# ===================================================================
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# ===================================================================
# WHATSAPP CLOUD API
# ===================================================================
whatsapp.api.base-url=https://graph.facebook.com/v25.0
whatsapp.webhook.verify-token=${WA_VERIFY_TOKEN:cok-gizli-dogrulama-kelimesi}

# ===================================================================
# WEB PUSH (VAPID)
# ===================================================================
push.public-key=${PUSH_VAPID_PUBLIC_KEY:}
push.private-key=${PUSH_VAPID_PRIVATE_KEY:}
push.subject=${PUSH_VAPID_SUBJECT:mailto:burakgules06@gmail.com}

# ===================================================================
# JWT - secret en az 32 karakter olmali (HMAC-SHA256 icin)
# ===================================================================
jwt.secret=${JWT_SECRET:bu-cok-gizli-bir-anahtar-en-az-32-karakter-olmali-1234}
jwt.expiration-ms=86400000
