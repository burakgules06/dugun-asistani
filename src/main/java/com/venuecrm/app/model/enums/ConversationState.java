package com.venuecrm.app.model.enums;

public enum ConversationState {
    IDLE,
    NAME_INPUT,
    GUEST_RANGE_SELECT,
    HALL_SELECT,
    MONTH_SELECT,
    WEEK_SELECT,
    MENU_SELECT,
    TIME_SLOT_SELECT,
    CONFIRM
}
